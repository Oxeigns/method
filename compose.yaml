services:
  bot:
    build: .
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - research_data:/app/data
    restart: unless-stopped
    read_only: true
    tmpfs: [/tmp]
    security_opt: [no-new-privileges:true]
    cap_drop: [ALL]
volumes:
  research_data:
